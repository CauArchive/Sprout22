#include <math.h>
#include <stdio.h>

int main() {
  int a = 1;
  int b = 2;
  printf("a + b 는 : %d\n", a + b);
  printf("a - b 는 : %d\n", a - b);
  printf("a * b 는 : %d\n", a * b);
  printf("a / b 는 : %d\n", a / b);
  return 0;
}