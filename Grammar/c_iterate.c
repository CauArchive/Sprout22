// Week 02
#include <math.h>
#include <stdio.h>

int main() {
  int a = 1;

  printf("a + 1 는 : %d\n", a + 1);
  printf("a + 2 는 : %d\n", a + 2);
  printf("a + 3 는 : %d\n", a + 3);
  printf("a + 4 는 : %d\n", a + 4);
  printf("a + 5 는 : %d\n", a + 5);
  printf("a + 6 는 : %d\n", a + 6);
  printf("...\n");
  printf("a + 100 는 : %d\n", a + 100);

  // for (int i = 0; i < 100; i++) {
  //   printf("a + %d 는 : %d\n", i, a + i);
  // }
  return 0;
}