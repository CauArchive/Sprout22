#include <math.h>
#include <stdio.h>

int main() {
  float a = 1;
  float b = 2;
  printf("a + b 는 : %f\n", a + b);
  printf("a - b 는 : %f\n", a - b);
  printf("a * b 는 : %f\n", a * b);
  printf("a / b 는 : %f\n", a / b);
  return 0;
}