#include <math.h>
#include <stdio.h>

int main() {
  // 몇 번 실행할지 제한할 변수 선언
  int number_of_input;
  // 정답을 출력할 변수 선언
  int answer;
  // 입력들을 전부 더할 변수 선언
  int sum;
  // n에 입력받기(채워넣으면 됩니다)
  scanf();
  // answer에 전부 입력받기(채워넣으면 됩니다)
  for (시작조건; 종료조건; 증감식) {
    // sum에 받은 입력 전부 더하기
  }
  // 정답 출력(채워넣으면 됩니다)
  printf("%d\n", );
  return 0;
}