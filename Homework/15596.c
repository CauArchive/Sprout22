#include <math.h>
#include <stdio.h>

// a = 합해야하는 정수 n개가 저장되어 있는 배열
// n = 합을 구해야하는 정수의 개수
long long sum(int* a, int n) {
  int sum = 0;
  // fill condition statement for "for iteration code"
  for (;;) {
    // do something here
  }
  return sum;
}